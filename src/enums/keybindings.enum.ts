/**
 * Enum representing key bindings for specific actions within a system,
 * such as player movement or general commands.
 *
 * This enumeration defines constants for various key binding scenarios,
 * providing an organized structure for managing input-related functionality.
 *
 */
export enum Keybindings {
    // player movement
    Accelerate,
    Brake,
    SteerLeft,
    SteerRight,
}
